{
    "name": "rs-hono-monorepo",
    "private": true,
    "packageManager": "pnpm@11.9.0",
    "workspaces": [
        "packages/*",
        "examples/*"
    ],
    "scripts": {
        "dev": "pnpm --filter rs-hono dev",
        "build": "pnpm --filter \"./packages/*\" build",
        "example:dev": "pnpm --filter rs-hono-example dev",
        "clean": "rm -rf packages/*/dist packages/*/node_modules examples/*/node_modules node_modules",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^3.9.1"
    }
}
