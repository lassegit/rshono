/**
 * Imported by the interactive route so `harness/build.mjs` has one file it can edit to force a real
 * incremental rebuild. Every app has this file at the same path.
 */
export const MARKER = 'bench';
