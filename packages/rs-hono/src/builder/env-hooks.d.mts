/**
 * Type declarations for env-hooks.mjs (kept as plain JS so Node's hooks
 * thread can load it without a TypeScript transform).
 */
export declare function registerEnvHooks(options: { rootDir: string; publicEnv: Record<string, string> }): void;
